import { DoubleArray } from 'cheminfo-types';

export interface DataXReIm {
  /** Array of x values */
  x: DoubleArray;
  /** Array of re values */
  re: DoubleArray;
  /** Array of im values */
  im: DoubleArray;
}
